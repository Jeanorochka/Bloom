# Python 3.10+ (из-за синтаксиса union: str | None)
numpy>=1.24
tqdm>=4.66
requests>=2.31

# криптография / ключи / кошельки
bip-utils>=2.9
eth-account>=0.9
PyNaCl>=1.5
base58>=2.1

# PyTorch
torch>=2.1    # под CPU; под CUDA ставь колёсико с pytorch.org
